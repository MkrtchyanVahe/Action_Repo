name: Hello_World workflow

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main      
    workflow_dispatch:

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: hello_world
              run: echo "hello World"
              shell: bash
    goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: goodbye world
              run: echo "Godbye world"
              shell: bash




